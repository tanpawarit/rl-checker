# คัดลอกเป็น .env แล้วเติมค่า — .env ถูก gitignore ห้าม commit key จริง
GEMINI_API_KEY=            # key จาก aistudio.google.com
GEMINI_MODEL=gemini-3.1-pro-preview
GEMINI_THINKING_LEVEL=     # ว่าง = default ของโมเดล (high) · ตั้งได้: minimal|low|medium|high
